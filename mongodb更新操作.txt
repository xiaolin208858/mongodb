@@ -1,27 +0,0 @@
更新：
    更新文本：db.集合名.update（{查询文档},{更新修改器：{键：值}}）
        修改器:
            $set表示修改已存在的键的值
            $inc:表示将键的值进行加减（注：只能对整数，浮点数进行加减，而且不存在隐式转换）
            $push:向数组中添加新的元素（只能添加单个元素）
            $addToSet:向数组中追加不重复的元素，默认仅能追加一个元素，必须和$each操作符配合才能追加多个元素
                （db.集合名.update({查询文档}，{$addToSet:{键：{$each:[值，值，值]}}})）



    对文本进行操作：db.集合名.update（{查询文档}，{修改器:{键：{操作符：值}}}）
        操作符：
            $each:向数组中添加多个元素
            $slice：向数组中添加元素，不会改变数组元素的个数，只保留最新的值，多则去掉
            $sort:向数组追加元素后对数组进行排序，{操作符：值，$sort:1升序/-1降序}
            $ne：保持数组中元素不会重复db.集合名.update（{键1：值，键2：{$ne:值}}，{$push:{键2：值}}）
            $sort：

    upsert修改器
        update的第三个参数,默认为false，表示不自动创建
        如果更新的文档不存在，true则自动创建，并把查询文档，更新文档拼接新文档插入
        db.集合名.update（{查询文档},{修改操作文档},true）
    $setOnInsert：在插入文档时，补充缺少的键
        (db.集合名.update（{查询文档},{其他修改器,$setOnInsert；{键：值}}，true）)
        $setOnInsert只有在update操作中，执行插入操作时才起作用，如果只更新，则无效
    save：函数将文档整体更新回集合时